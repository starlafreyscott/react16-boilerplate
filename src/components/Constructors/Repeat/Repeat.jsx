import React from 'react';

function Repeat(Xelement, json, reactKey){
    // take an element map the json into the element use the key and return the chunk

    return (
        <Xelement/>
    )
}

function mashup(){
    // take a comp and repeat it
}



/*
* [] Should take in a component
* [] Should take in JSON as a shape
* [] Should pass JSON to elements
* []
*
 */